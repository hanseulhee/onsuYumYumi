import { Theme } from "@emotion/react";

const theme: Theme = {
  color: {
    yellow: "#fbc531",
    grey: "#f2f2f2",
  },
  fontWeight: {
    light: 100,
    normal: 400,
    bold: 700,
  },
};

export default theme;
